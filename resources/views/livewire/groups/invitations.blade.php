<section class="mx-auto max-w-3xl space-y-6"><x-app.page-header :title="$group->name.' invitations'" /><flux:card><form wire:submit="create" class="grid grid-cols-1 gap-3 sm:grid-cols-2"><flux:input wire:model="email" label="Target email (optional)" /><flux:input wire:model="maxUses" type="number" label="Maximum uses" /><flux:button type="submit" variant="primary">Create invitation</flux:button></form></flux:card><div class="space-y-3">@foreach($invitations as $invitation)<flux:card class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><flux:text class="break-all">{{ route('invitations.show', $invitation->token) }} · uses {{ $invitation->uses_count }}/{{ $invitation->max_uses ?? '∞' }}</flux:text>@if(!$invitation->revoked_at)<flux:button wire:click="revoke({{ $invitation->id }})" variant="danger" size="sm">Revoke</flux:button>@endif</flux:card>@endforeach</div></section>
