<section class="mx-auto max-w-2xl space-y-6"><x-app.page-header title="Admission" :description="'Current status: '.$admission->status" /><flux:card class="space-y-4"><flux:textarea wire:model="note" label="Message or review note" rows="3" />@can('update', $admission)<div class="flex flex-col gap-2 sm:flex-row"><flux:button wire:click="submit" variant="primary">Submit admission</flux:button></div>@endcan</flux:card><flux:card class="space-y-3"><flux:heading size="lg">Required agreements</flux:heading>@foreach($versions as $version)<div class="flex flex-col gap-2 border-b pb-3 sm:flex-row sm:items-center sm:justify-between"><flux:text>{{ $version->agreement->name }} v{{ $version->version }}</flux:text>@can('update', $admission)<flux:button wire:click="acceptVersion({{ $version->id }})" size="sm">Accept version</flux:button>@endcan</div>@endforeach</flux:card>@can('manageAdmissions', $admission->group)<flux:card class="space-y-3"><flux:heading size="lg">Review</flux:heading><div class="grid grid-cols-1 gap-2 sm:grid-cols-2"><flux:button wire:click="review('clarification_required')">Request clarification</flux:button><flux:button wire:click="review('under_review')">Start review</flux:button><flux:button wire:click="review('approved')" variant="primary">Approve</flux:button><flux:button wire:click="review('rejected')" variant="danger">Reject</flux:button></div><flux:button wire:click="finalize" variant="primary" class="w-full">Finalize approved admission</flux:button></flux:card>@endcan</section>
